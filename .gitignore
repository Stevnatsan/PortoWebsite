# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Claude Code local settings
.claude/settings.local.json

# Node (if ever added)
node_modules/
npm-debug.log*

# Vercel CLI link data
.vercel

# Secrets — never commit tokens (GITHUB_TOKEN lives in Vercel's dashboard)
.env
.env.*
